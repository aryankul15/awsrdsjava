version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "threetier"
        LoadBalancerInfo:
          ContainerName: "tomcat"
          ContainerPort: 8080
